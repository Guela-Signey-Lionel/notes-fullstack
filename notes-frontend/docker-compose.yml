version: '3.9'
services:
  frontend:
    build: .
    container_name: notes-frontend
    ports:
      - "4201:80"
    environment:
      BACKEND_URL: ${BACKEND_URL:-http://localhost:8081}
    restart: unless-stopped
